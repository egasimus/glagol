(lambda (s) (return
  (?: s.results.length
    (./h "ul.results"
      (s.results.map (lambda (result) (return
        (./h "li.result"
          (object "onclick" (./emit "add-track" result))
          result))))) null)))
