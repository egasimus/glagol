(let [state
        { :meter    nil
          :browser  (.sync (require "glob") ./options/sounds-path)
          :samplers [] }]
  (./events/on :add-sample (fn [id] (state.samplers.push (aget state.browser id))))
  state)
