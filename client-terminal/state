(let [glob
        (require "glob")
      path
        (require "path")
      state
        { :meter
            nil
          :browser
            (.map (glob.sync ./options/sounds-path)
              (fn [f] (path.basename f))) }]
  state)
