(lambda ()
  (console.log "adding sampler")
  (return ((. (./init) then)
    (lambda (sampler)
      (var samplers (./state))
      (= (get samplers sampler.id) sampler)
      (./state/set samplers)
      (return sampler)))))
