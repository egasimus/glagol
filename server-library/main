(let [path (require "path")
      glob (require "glob")
      fs   (require "fs")]
  (log ./options/soxi-path)
  (.map (glob.sync (path.join ./options/sounds-dir "**" "*.wav"))
    (fn [f] (log [ (path.relative ./options/sounds-dir f)
                   (./soxi-analyze f) ]))))
