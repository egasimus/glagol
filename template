(fn template-app []
  (.h @vdom ".app" [
    @template-toolbar
    (@template-sequencer "Kick"  8)
    (@template-sequencer "Snare" 8)
    (@template-sequencer "Hihat" 16)])))
    ;(h ".samplers" [
      ;(@template-sampler 1)
      ;(@template-sampler 2)])])))
