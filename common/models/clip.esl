(lambda (data)
  (= data (|| data (object)))
  (var observ ../lib/observ)
  (return  (observ.Obj
    "id"   (observ.Var (|| data.id ((. (require "shortid") generate))))
    "path" (observ.Var data.path)
    "cues" (observ.Arr))))
