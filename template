(fn [state]
  (console.log state)
  (let [h (.-h @vdom)]
    (h ".app" [
      @template-toolbar
      (h ".samplers" (@template-samplers))
      (@template-search state)
      (@template-results)])))
