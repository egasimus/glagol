(let [state { :jack-up false }]

  (./server/socket/on :open (fn []
    (-> ./server/api
      (.get "sequencer")
      (.invoke "jack-up")
      (.then (fn []
        (set! state.jack-up true)
        (./render))))))

  state)
