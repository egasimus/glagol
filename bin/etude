#!/usr/bin/env node

var app = require('glagol')(
  require('path').resolve(__dirname, '../server'),
  { eslisp:
    { transform:
      [ require('eslisp-camelify')
      , require('eslisp-propertify') ] }});

app.tree()['main'](app);
