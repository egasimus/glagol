(fn template-app []
  (.h @vdom ".app" [
    @template-toolbar
    (@template-pads 16)
    (@template-sequencer :kick  (.-length (.-kick  @state)))
    (@template-sequencer :snare (.-length (.-snare @state)))
    (@template-sequencer :hihat (.-length (.-hihat @state)))]))
