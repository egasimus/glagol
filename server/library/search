(fn search [query]
  (log.as :search query)
  (let [fltr  (fn [snd] (not (= -1 (.index-of (snd.to-lower-case) (GET.q.to-lower-case)))))
        results (if query (./data/filter fltr) data)]
    (log.as :found results.length)
    (JSON.stringify results)))
