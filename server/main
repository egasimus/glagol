#!/usr/bin/env etude
(let [server  (new (.-Server (require :ws)) { :port 1620 })]

  (log.as :hello "starting with modules:" (.join (keys ./api) " "))

  (server.on :connection (fn [socket]
    (log.as :connection "")
    ((require :q-connection) socket ./api))))
