(fn [document]
  (./vdom/insert-css (.readFileSync (require :fs) "./client/web/style.styl" :utf8))
  (./vdom/update (./view document) ./state))
  ;(debug "\nindex" ./state)
  ;(./vdom/update ./view ./state)) ; i'm not sure why this works
                                  ;; or if
