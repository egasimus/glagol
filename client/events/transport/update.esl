(lambda (results)
  (if (=== "string" (typeof results)) (= results (JSON.parse results)))
  ;(console.log "transport" results)
  (var state (../../state))
  (= state.transport results)
  (../../state/set state))
