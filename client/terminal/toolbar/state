(let [state { :jack-up false }]

  (-> (../api :invoke "sequencer/jack-up")
    (.then (fn []
      (set! state.jack-up true)
      (./render ../screen state))))

    ;(-> ../server/api
      ;(.get "sequencer")
      ;(.invoke "jack-up")
      ;(.then (fn []
        ;(set! state.jack-up true)
        ;(./render ../screen state))))))

  state)
