(let [NanoTimer (require "nanotimer")]
  { :each
      (fn [t f]
        (let [timer (NanoTimer.)]
          (timer.set-interval f [] t)
          { :timer timer
            :undo  (fn [] (timer.clear-interval)) })) })
