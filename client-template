(fn [s]
  (let [h (.-h @lib-vdom)]
    (h ".app"
      [ (h "a.play" { :onclick (@lib-emit "transport.play") } "▶")
        (@client-template-sequencer :hihat)
        (@client-template-sequencer :kick)
        (@client-template-sequencer :snare) ])))
