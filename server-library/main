(let [path (require "path")
      glob (require "glob")
      fs   (require "fs")]
  (log ./options/soxi-path)
  (log (.map (glob.sync (path.join ./options/sounds-dir "**" "*.wav"))
    (fn [f] [ (path.relative ./options/sounds-dir f) 0 ]))))
