(lambda (s)
  (var options  (object))
  (var elements (array))
  ((. (Object.keys s.sequencers) map)
    (lambda (id) (elements.push (./sequencer (get s.sequencers id)))))
  (return (./h ".session" options elements)))
