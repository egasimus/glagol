(lambda (results)
  (if (=== "string" (typeof results)) (= results (JSON.parse results)))
  (console.log "samplers" results)
  (var samplers (../../state/samplers))
  (.map (Object.keys results) (lambda (id)
    (= (get samplers id) (../../models/sampler (get results id)))))
  (../../state/samplers/set samplers))
