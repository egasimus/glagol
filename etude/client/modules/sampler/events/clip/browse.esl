(lambda (sampler-id clip-id)
  (var arg (array sampler-id clip-id))
  (.done (/api "session/add" "library" arg)
    (lambda (data) (console.log data))
    (/lib/error (+ "could not open library for " arg))))
