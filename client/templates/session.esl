(lambda (s)
  (var options  (object))
  (var elements (array))
  (console.log s.sequencers)
  (elements.concat ((. (Object.keys s.sequencers) map) ./sequencer))
  (console.log elements)
  (return (./h ".session" options elements)))
