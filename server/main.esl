(lambda main (app)

  (var host "0.0.0.0")
  (var port 1620)

  (var server (/web/server host port /routes))

  (server.http.on "listening" (lambda ()
    (console.log "open" (+ host ":" port) "in your browser")))
;    ((require "opener") (+ "http://" host ":" port))))

  (server.socket.on "connection" (lambda (socket)
    (./connect socket))))
