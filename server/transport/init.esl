((lambda ()

  (function stringify (state)
    (return (JSON.stringify state (lambda (key val)
      (return val)))))

  (return (lambda (cb)
    (if cb (./state cb))
    (return (stringify (./state)))))

))
