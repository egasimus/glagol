(fn [opts]
  (let [defaults
          { :width 25 :focused true :mouse true
            :style { :bg :black :focus { :bg :darkgray :fg :yellow } } }
        l
          (../bless/List (merge defaults opts))]
    (l.key [:down  :j] (fn [] (l.down 1)))
    (l.key [:up    :k] (fn [] (l.up   1)))
    (l.key [:enter :a] (fn [] (../events/emit :add-sample l.selected)))
    l))

